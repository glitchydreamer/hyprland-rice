---
name: user-profile
description: "Who the user is — robotics/ML dev on a Hyprland+caelestia rice, ex-Ubuntu, fish/terminal-first"
metadata: 
  node_type: memory
  type: user
  originSessionId: 3e580e21-3aa2-41e4-b07c-1d0cc9cca6b4
---

Personal-workstation owner running a multi-distro **Hyprland + caelestia** rice
(repo `glitchydreamer/hyprland-rice`). The **live box is CachyOS**; an Arch build
is kept in parity. A **roaming NVMe** also boots a laptop (Intel + RTX 4070
Mobile, eDP-1); configs must work on both hosts, switching automatically.

- **Work:** robotics / ML dev — Isaac Sim/Lab, ROS 2 Humble + MoveIt 2, CUDA,
  embedded serial, LeRobot for a real SO-arm 101. GPU is an RTX 3060 (desktop).
- **Shell/UX:** terminal-first, **fish** shell, LG 34" ultrawide on DP-1.
- **Background:** came from **Ubuntu/GNOME** — explanations often benefit from an
  Ubuntu→Arch framing; docs target that audience.
- **Git identity for commits:** GitHub account `glitchydreamer`. Recover the
  exact `user.email` on any machine with `git config user.email` (it is NOT
  the Claude-context email). Kept out of this mirrored file on purpose, since
  `.agent-memory/` lives in a public repo.

See [[project-cachyos-live-stack]], [[feedback-maintenance-loop]],
[[feedback-sudo-real-terminal]].
